rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write access during development
    // TODO: Change these rules before deploying to production
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
